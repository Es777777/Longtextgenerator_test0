# 请不要在仓库中提交真实密钥；本文件作为模板使用。
# 将真实密钥放在安全的环境变量管理或运行时注入处。
LLM_API_KEY=
LLM_ENABLE=true
# 默认通用接口（可覆盖）：
LLM_BASE_URL=https://api.minimaxi.com/anthropic
LLM_API_KEY_ENV=LLM_API_KEY
LLM_MODEL=your-model-name
LLM_TIMEOUT_SECONDS=60
LLM_MAX_RETRIES=2

# Anthropic 专用：可按地理位置选择基础URL（优先级高于 LLM_BASE_URL）
# ANTHROPIC_BASE_URL: 如果设置则直接使用该完整地址
# ANTHROPIC_REGION: 可设置为 "domestic"/"cn" 或 "international"/"intl"
ANTHROPIC_BASE_URL=
ANTHROPIC_REGION=
LLM_GENERATE_PATH=
# 认证类型: 'bearer' 使用 Authorization: Bearer <key>; 若需使用自定义头请填写头名, 例如: x-api-key
LLM_AUTH_TYPE=bearer

# Deepseek 示例配置：如果你要使用 Deepseek，将基础 URL 指向下面地址（可覆盖默认 LLM_BASE_URL）。
# 说明：前往 Deepseek 官方站点申请 API Key（例如 https://deepseek.ai 或 https://api.deepseek.com），
# 然后把密钥注入运行时环境（可使用通用变量 `LLM_API_KEY`，或设置 `DEEPSEEK_API_KEY` 并在代码中读取）。
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY_ENV=LLM_API_KEY

PERPLEXITY_ENABLE=false
PERPLEXITY_ENDPOINT=https://your-llm-provider/v1/logprobs
PERPLEXITY_TEXT_FIELD=prompt
PERPLEXITY_LOGPROBS_FIELD=logprobs

MAX_CHUNK_CHARS=800
OVERLAP_CHARS=80
ENABLE_OVERLAP=true
SUMMARY_CHARS=120
ENABLE_SELF_CHECK=true

AST_GREP_ENABLE=true
AST_GREP_COMMAND=sg
AST_GREP_LANGUAGE=python
AST_GREP_PATTERNS=(function_definition),(class_definition)

TEXT_TYPE_MIN_SCORE=3
TEXT_TYPE_LINE_RATIO_DIVISOR=3
TEXT_TYPE_KEYWORD_WEIGHT=2
TEXT_TYPE_SYMBOL_WEIGHT=1
TEXT_TYPE_LINE_WEIGHT=2
TEXT_TYPE_KEYWORD_PATTERN=\b(def|class|import|from|return|if|else|for|while|try|except|function|const|let|var|public|private|#include)\b
TEXT_TYPE_SYMBOL_PATTERN=[{}();=<>\[\]]
TEXT_TYPE_LINE_START_PATTERN=^\s*(def|class|import|from|#include)\b
TEXT_TYPE_CALL_LIKE_PATTERN=^\s*[^\s]+\s*\([^)]*\)\s*\{?\s*$
TEXT_TYPE_COMMENT_PATTERN=^\s*//|^\s*#
